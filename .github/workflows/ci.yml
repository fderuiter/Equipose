name: CI Workflow

on:
  push:
    branches:
    - main
  pull_request:
jobs:
  setup:
    runs-on: ubuntu-latest

    steps:
    - name: Harden Runner
      uses: step-security/harden-runner@9af89fc71515a100421586dfdb3dc9c984fbf411
      with:
        egress-policy: audit
        allowed-endpoints: |-
          github.com:443
          api.github.com:443
          objects.githubusercontent.com:443
          registry.npmjs.org:443
          registry.yarnpkg.com:443
          nodejs.org:443
          *.blob.core.windows.net:443
          *.actions.githubusercontent.com:443
          playwright.azureedge.net:443
          archive.ubuntu.com:80
          security.ubuntu.com:80
          cdn.playwright.dev:443
          playwright.download.prss.microsoft.com:443
          storage.googleapis.com:443
    - name: Checkout repository
      uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683

    - name: Install pnpm
      uses: pnpm/action-setup@fc06bc1257f339d1d5d8b3a19a8cae5388b55320
      with:
        run_install: false

    - name: Set up Node.js
      uses: actions/setup-node@39370e3970a6d050c480ffad4ff0ed4d3fdee5af
      with:
        node-version: '22'
        cache: 'pnpm'

    - name: Install Dependencies
      run: pnpm install --frozen-lockfile

    - name: Build Project
      run: pnpm run build

    - name: Install Playwright Browsers
      run: npx playwright install --with-deps
      env:
        PLAYWRIGHT_BROWSERS_PATH: ${{ github.workspace }}/pw-browsers

    - name: Run Unit Tests
      run: pnpm test --watch=false

    - name: Archive environment
      run: tar -cf shared-env.tar node_modules pw-browsers dist

    - name: Upload shared environment
      uses: actions/upload-artifact@65462800fd760344b1a7b4382951275a0abb4808
      with:
        name: shared-env
        path: shared-env.tar
        retention-days: 1

    permissions:
      contents: read
  e2e:
    runs-on: ubuntu-latest
    needs: setup
    permissions:
      contents: read

    strategy:
      fail-fast: false
      matrix:
        shardIndex: [1, 2, 3, 4]
        shardTotal: [4]

    steps:
    - name: Harden Runner
      uses: step-security/harden-runner@9af89fc71515a100421586dfdb3dc9c984fbf411
      with:
        egress-policy: audit
        allowed-endpoints: |-
          github.com:443
          api.github.com:443
          objects.githubusercontent.com:443
          registry.npmjs.org:443
          registry.yarnpkg.com:443
          nodejs.org:443
          *.blob.core.windows.net:443
          *.actions.githubusercontent.com:443
          playwright.azureedge.net:443
          archive.ubuntu.com:80
          security.ubuntu.com:80
    - name: Checkout repository
      uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683

    - name: Set up Node.js
      uses: actions/setup-node@39370e3970a6d050c480ffad4ff0ed4d3fdee5af
      with:
        node-version: '22'

    - name: Download shared environment
      uses: actions/download-artifact@65a9edc5881444af0b9093a5e628f2fe47ea3b2e
      with:
        name: shared-env
        path: .

    - name: Extract shared environment
      run: tar -xf shared-env.tar

    - name: Run E2E Tests (Shard ${{ matrix.shardIndex }}/${{ matrix.shardTotal 
        }})
      run: npx playwright test --shard=${{ matrix.shardIndex }}/${{ 
        matrix.shardTotal }}
      env:
        CI: true
        PLAYWRIGHT_BROWSERS_PATH: ${{ github.workspace }}/pw-browsers

    - name: Upload blob report
      uses: actions/upload-artifact@65462800fd760344b1a7b4382951275a0abb4808
      if: always()
      with:
        name: blob-report-${{ matrix.shardIndex }}
        path: blob-report/
        retention-days: 1

  code_generation_fixtures:
    runs-on: ubuntu-latest
    needs: setup
    permissions:
      contents: read

    steps:
    - name: Harden Runner
      uses: step-security/harden-runner@9af89fc71515a100421586dfdb3dc9c984fbf411
      with:
        egress-policy: audit
        allowed-endpoints: |-
          github.com:443
          api.github.com:443
          objects.githubusercontent.com:443
          registry.npmjs.org:443
          registry.yarnpkg.com:443
          nodejs.org:443
          *.blob.core.windows.net:443
          *.actions.githubusercontent.com:443
          playwright.azureedge.net:443
          archive.ubuntu.com:80
          security.ubuntu.com:80
    - name: Checkout repository
      uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683

    - name: Set up Node.js
      uses: actions/setup-node@39370e3970a6d050c480ffad4ff0ed4d3fdee5af
      with:
        node-version: '22'

    - name: Set up Python
      uses: actions/setup-python@82c7e631bb3cdc910f68e0081d67478d79c6982d
      with:
        python-version: '3.11'

    - name: Set up R
      uses: r-lib/actions/setup-r@d3c5be51b12e724e68f33216ca3c148b66d5f0b6
      with:
        r-version: 'release'

    - name: Install Python dependencies for generated scripts
      run: pip install numpy pandas --quiet

    - name: Download shared environment
      uses: actions/download-artifact@65a9edc5881444af0b9093a5e628f2fe47ea3b2e
      with:
        name: shared-env
        path: .

    - name: Extract shared environment
      run: tar -xf shared-env.tar

    - name: Export code-generation fixture scripts
      run: npx playwright test tests_e2e/code-generation-fixture.spec.ts
      env:
        CI: true
        CODEGEN_FIXTURES: '1'
        PYTHON: python3
        PLAYWRIGHT_BROWSERS_PATH: ${{ github.workspace }}/pw-browsers

    - name: Upload code-generation fixture scripts
      uses: actions/upload-artifact@65462800fd760344b1a7b4382951275a0abb4808
      if: always()
      with:
        name: code-generation-fixtures
        path: artifacts/code-generation-fixtures/
        retention-days: 30

  python_script_execution_check:
    runs-on: ubuntu-latest
    needs: code_generation_fixtures
    permissions:
      contents: read

    steps:
    - name: Harden Runner
      uses: step-security/harden-runner@9af89fc71515a100421586dfdb3dc9c984fbf411
      with:
        egress-policy: audit
        allowed-endpoints: |-
          github.com:443
          api.github.com:443
          objects.githubusercontent.com:443
          registry.npmjs.org:443
          registry.yarnpkg.com:443
          nodejs.org:443
          *.blob.core.windows.net:443
          *.actions.githubusercontent.com:443
          playwright.azureedge.net:443
          archive.ubuntu.com:80
          security.ubuntu.com:80
    - name: Checkout repository
      uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683

    - name: Set up Python
      uses: actions/setup-python@82c7e631bb3cdc910f68e0081d67478d79c6982d
      with:
        python-version: '3.11'

    - name: Install Python dependencies
      run: pip install numpy pandas --quiet

    - name: Download code-generation fixture scripts
      uses: actions/download-artifact@65a9edc5881444af0b9093a5e628f2fe47ea3b2e
      with:
        name: code-generation-fixtures
        path: artifacts/code-generation-fixtures/

    - name: Execute generated Python scripts
      run: |
        set -e
        PASS=0
        FAIL=0
        ERRORS=""
        echo "=== Running UI-generated Python scripts ==="
        while IFS= read -r -d '' script; do
          echo ""
          echo "--- Running: $script ---"
          if python3 "$script"; then
            echo "PASS: $script"
            PASS=$((PASS + 1))
          else
            echo "FAIL: $script"
            FAIL=$((FAIL + 1))
            ERRORS="$ERRORS\n  ✗ $script"
          fi
        done < <(find artifacts/code-generation-fixtures -name '*.py' -print0 | sort -z)
        echo ""
        echo "=== Python Script Execution Summary ==="
        echo "  Passed: $PASS"
        echo "  Failed: $FAIL"
        if [ "$FAIL" -gt 0 ]; then
          echo ""
          echo "FAILED scripts:"
          printf '%b\n' "$ERRORS"
          exit 1
        fi
        if [ "$PASS" -eq 0 ]; then
          echo "ERROR: No Python scripts found in artifacts/code-generation-fixtures/"
          exit 1
        fi
        echo "All $PASS generated Python script(s) executed successfully."

  sas_static_validation:
    runs-on: ubuntu-latest
    needs: code_generation_fixtures
    permissions:
      contents: read

    steps:
    - name: Harden Runner
      uses: step-security/harden-runner@9af89fc71515a100421586dfdb3dc9c984fbf411
      with:
        egress-policy: audit
        allowed-endpoints: |-
          github.com:443
          api.github.com:443
          objects.githubusercontent.com:443
          registry.npmjs.org:443
          registry.yarnpkg.com:443
          nodejs.org:443
          *.blob.core.windows.net:443
          *.actions.githubusercontent.com:443
          playwright.azureedge.net:443
          archive.ubuntu.com:80
          security.ubuntu.com:80
    - name: Checkout repository
      uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683

    - name: Set up Node.js
      uses: actions/setup-node@39370e3970a6d050c480ffad4ff0ed4d3fdee5af
      with:
        node-version: '22'

    - name: Download shared environment
      uses: actions/download-artifact@65a9edc5881444af0b9093a5e628f2fe47ea3b2e
      with:
        name: shared-env
        path: .

    - name: Extract shared environment
      run: tar -xf shared-env.tar

    - name: Download code-generation fixture scripts
      uses: actions/download-artifact@65a9edc5881444af0b9093a5e628f2fe47ea3b2e
      with:
        name: code-generation-fixtures
        path: artifacts/code-generation-fixtures/

    - name: Run SAS static syntax validator
      run: node scripts/validate-sas-syntax.mjs

  stata_static_validation:
    runs-on: ubuntu-latest
    needs: code_generation_fixtures
    permissions:
      contents: read

    steps:
    - name: Harden Runner
      uses: step-security/harden-runner@9af89fc71515a100421586dfdb3dc9c984fbf411
      with:
        egress-policy: audit
        allowed-endpoints: |-
          github.com:443
          api.github.com:443
          objects.githubusercontent.com:443
          registry.npmjs.org:443
          registry.yarnpkg.com:443
          nodejs.org:443
          *.blob.core.windows.net:443
          *.actions.githubusercontent.com:443
          playwright.azureedge.net:443
          archive.ubuntu.com:80
          security.ubuntu.com:80
    - name: Checkout repository
      uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683

    - name: Set up Node.js
      uses: actions/setup-node@39370e3970a6d050c480ffad4ff0ed4d3fdee5af
      with:
        node-version: '22'
        cache: 'pnpm'

    - name: Download code-generation fixture scripts
      uses: actions/download-artifact@65a9edc5881444af0b9093a5e628f2fe47ea3b2e
      with:
        name: code-generation-fixtures
        path: artifacts/code-generation-fixtures/

    - name: Run Stata static syntax validator
      run: node scripts/validate-stata-syntax.mjs

  merge_reports:
    runs-on: ubuntu-latest
    needs: e2e
    if: !cancelled() && needs.e2e.result != 'skipped'
    permissions:
      contents: read

    steps:
    - name: Harden Runner
      uses: step-security/harden-runner@9af89fc71515a100421586dfdb3dc9c984fbf411
      with:
        egress-policy: audit
        allowed-endpoints: |-
          github.com:443
          api.github.com:443
          objects.githubusercontent.com:443
          registry.npmjs.org:443
          registry.yarnpkg.com:443
          nodejs.org:443
          *.blob.core.windows.net:443
          *.actions.githubusercontent.com:443
          playwright.azureedge.net:443
          archive.ubuntu.com:80
          security.ubuntu.com:80
    - name: Checkout repository
      uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683

    - name: Set up Node.js
      uses: actions/setup-node@39370e3970a6d050c480ffad4ff0ed4d3fdee5af
      with:
        node-version: '22'

    - name: Download shared environment
      uses: actions/download-artifact@65a9edc5881444af0b9093a5e628f2fe47ea3b2e
      with:
        name: shared-env
        path: .

    - name: Extract shared environment
      run: tar -xf shared-env.tar

    - name: Download all blob reports
      uses: actions/download-artifact@65a9edc5881444af0b9093a5e628f2fe47ea3b2e
      with:
        path: all-blob-reports
        pattern: blob-report-*
        merge-multiple: true

    - name: Merge Playwright reports
      run: |
        npx playwright merge-reports --reporter html ./all-blob-reports
        mkdir -p playwright-report
        npx playwright merge-reports --reporter json ./all-blob-reports > playwright-report/report.json

    - name: Upload merged HTML report
      uses: actions/upload-artifact@65462800fd760344b1a7b4382951275a0abb4808
      with:
        name: playwright-report
        path: playwright-report/
        retention-days: 30

  traceability_matrix:
    runs-on: ubuntu-latest
    needs: [setup, merge_reports, python_script_execution_check, sas_static_validation, stata_static_validation, cross_env_equivalence, sbom, security_scan, lint_actions]
    if: !cancelled() && needs.setup.result == 'success'
    permissions:
      contents: read

    steps:
    - name: Harden Runner
      uses: step-security/harden-runner@9af89fc71515a100421586dfdb3dc9c984fbf411
      with:
        egress-policy: audit
        allowed-endpoints: |-
          github.com:443
          api.github.com:443
          objects.githubusercontent.com:443
          registry.npmjs.org:443
          registry.yarnpkg.com:443
          nodejs.org:443
          *.blob.core.windows.net:443
          *.actions.githubusercontent.com:443
          playwright.azureedge.net:443
          archive.ubuntu.com:80
          security.ubuntu.com:80
          pypi.org:443
          files.pythonhosted.org:443
    - name: Checkout repository
      uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683

    - name: Set up Node.js
      uses: actions/setup-node@39370e3970a6d050c480ffad4ff0ed4d3fdee5af
      with:
        node-version: '22'

    - name: Set up Python
      uses: actions/setup-python@82c7e631bb3cdc910f68e0081d67478d79c6982d
      with:
        python-version: '3.11'

    - name: Install Python dependencies
      run: pip install numpy pandas --quiet

    - name: Download shared environment
      uses: actions/download-artifact@65a9edc5881444af0b9093a5e628f2fe47ea3b2e
      with:
        name: shared-env
        path: .

    - name: Extract shared environment
      run: tar -xf shared-env.tar

    - name: Run unit tests with JSON reporter
      run: |
        node generate-version.js
        ./node_modules/.bin/vitest run \
          --exclude 'tests_e2e/**' \
          --reporter=json \
          --outputFile=vitest-results.json || true

    - name: Download Playwright JSON report
      uses: actions/download-artifact@65a9edc5881444af0b9093a5e628f2fe47ea3b2e
      with:
        name: playwright-report
        path: playwright-report/
      continue-on-error: true

    - name: Generate Validation Traceability Matrix
      run: |
        cat << 'EOF' > job-statuses.json
        {
          "python_script_execution_check": "${{ needs.python_script_execution_check.result }}",
          "sas_static_validation": "${{ needs.sas_static_validation.result }}",
          "stata_static_validation": "${{ needs.stata_static_validation.result }}",
          "cross_env_equivalence": "${{ needs.cross_env_equivalence.result }}",
          "sbom": "${{ needs.sbom.result }}",
          "security_scan": "${{ needs.security_scan.result }}",
          "lint_actions": "${{ needs.lint_actions.result }}"
        }
        EOF
        VITEST_ARG=""
        PW_ARG=""
        if [ -f vitest-results.json ]; then
          VITEST_ARG="--vitest-results vitest-results.json"
        fi
        if [ -f playwright-report/report.json ]; then
          PW_ARG="--playwright-results playwright-report/report.json"
        fi
        node scripts/generate-rtm.mjs "$VITEST_ARG" "$PW_ARG" \
          --ci-results job-statuses.json \
          --out Validation_Traceability_Matrix.md
        echo "=== Traceability Matrix (first 50 lines) ==="
        head -50 Validation_Traceability_Matrix.md

    - name: Upload Validation Traceability Matrix
      uses: actions/upload-artifact@65462800fd760344b1a7b4382951275a0abb4808
      with:
        name: validation-traceability-matrix
        path: Validation_Traceability_Matrix.md
        retention-days: 90

  script_validation_report:
    runs-on: ubuntu-latest
    needs: [python_script_execution_check, sas_static_validation, 
        stata_static_validation, cross_env_equivalence]
    if: !cancelled()
    permissions:
      contents: read

    steps:
    - name: Harden Runner
      uses: step-security/harden-runner@9af89fc71515a100421586dfdb3dc9c984fbf411
      with:
        egress-policy: audit
        allowed-endpoints: |-
          github.com:443
          api.github.com:443
          objects.githubusercontent.com:443
          registry.npmjs.org:443
          registry.yarnpkg.com:443
          nodejs.org:443
          *.blob.core.windows.net:443
          *.actions.githubusercontent.com:443
          playwright.azureedge.net:443
          archive.ubuntu.com:80
          security.ubuntu.com:80
    - name: Checkout repository
      uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683

    - name: Set up Node.js
      uses: actions/setup-node@39370e3970a6d050c480ffad4ff0ed4d3fdee5af
      with:
        node-version: '22'

    - name: Download code-generation fixture scripts
      uses: actions/download-artifact@65a9edc5881444af0b9093a5e628f2fe47ea3b2e
      with:
        name: code-generation-fixtures
        path: artifacts/code-generation-fixtures/
      continue-on-error: true

    - name: Generate script validation report
      run: |
        node scripts/generate-script-validation-report.mjs \
          --python-result "${{ needs.python_script_execution_check.result }}" \
          --r-result      "${{ needs.cross_env_equivalence.result }}" \
          --sas-result    "${{ needs.sas_static_validation.result }}" \
          --stata-result  "${{ needs.stata_static_validation.result }}" \
          --fixture-root  artifacts/code-generation-fixtures \
          --out           script-validation-report.md
        echo "=== Script Validation Report (preview) ==="
        head -40 script-validation-report.md

    - name: Upload script validation report
      uses: actions/upload-artifact@65462800fd760344b1a7b4382951275a0abb4808
      if: always()
      with:
        name: script-validation-report
        path: script-validation-report.md
        retention-days: 90

  cross_env_equivalence:
    runs-on: ubuntu-latest
    needs: [setup, code_generation_fixtures]
    permissions:
      contents: read

    steps:
    - name: Harden Runner
      uses: step-security/harden-runner@9af89fc71515a100421586dfdb3dc9c984fbf411
      with:
        egress-policy: audit
        allowed-endpoints: |-
          github.com:443
          api.github.com:443
          objects.githubusercontent.com:443
          registry.npmjs.org:443
          registry.yarnpkg.com:443
          nodejs.org:443
          *.blob.core.windows.net:443
          *.actions.githubusercontent.com:443
          playwright.azureedge.net:443
          archive.ubuntu.com:80
          security.ubuntu.com:80
    - name: Checkout repository
      uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683

    - name: Set up Python
      uses: actions/setup-python@82c7e631bb3cdc910f68e0081d67478d79c6982d
      with:
        python-version: '3.11'

    - name: Set up R
      uses: r-lib/actions/setup-r@d3c5be51b12e724e68f33216ca3c148b66d5f0b6
      with:
        r-version: 'release'

    - name: Install Python dependencies
      run: pip install numpy --quiet

    - name: Download code-generation fixture scripts
      uses: actions/download-artifact@65a9edc5881444af0b9093a5e628f2fe47ea3b2e
      with:
        name: code-generation-fixtures
        path: artifacts/code-generation-fixtures/

    - name: Python Cross-Environment Equivalence Check
      run: |
        echo "Running Python cross-environment equivalence fixture..."
        python3 scripts/cross-env/verify_python_schema.py
        echo "Python equivalence check completed."

    - name: R Cross-Environment Equivalence Check
      run: |
        echo "Running R cross-environment equivalence fixture and generated-script execution checks..."
        Rscript scripts/cross-env/verify_r_schema.R artifacts/code-generation-fixtures
        echo "R equivalence and generated-script checks completed."

  sbom:
    runs-on: ubuntu-latest
    needs: setup
    permissions:
      contents: read

    steps:
    - name: Harden Runner
      uses: step-security/harden-runner@9af89fc71515a100421586dfdb3dc9c984fbf411
      with:
        egress-policy: audit
        allowed-endpoints: |-
          github.com:443
          api.github.com:443
          objects.githubusercontent.com:443
          registry.npmjs.org:443
          registry.yarnpkg.com:443
          nodejs.org:443
          *.blob.core.windows.net:443
          *.actions.githubusercontent.com:443
          playwright.azureedge.net:443
          archive.ubuntu.com:80
          security.ubuntu.com:80
    - name: Checkout repository
      uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683

    - name: Set up Node.js
      uses: actions/setup-node@39370e3970a6d050c480ffad4ff0ed4d3fdee5af
      with:
        node-version: '22'

    - name: Download shared environment
      uses: actions/download-artifact@65a9edc5881444af0b9093a5e628f2fe47ea3b2e
      with:
        name: shared-env
        path: .

    - name: Extract shared environment
      run: tar -xf shared-env.tar

    - name: Generate Software Bill of Materials (SBOM)
      # [REQ-SBOM-001]
      run: |
        echo "Generating CycloneDX SBOM for supply chain audit..."
        npx --yes @cyclonedx/cdxgen \
                    -o sbom.json
        echo "SBOM generated successfully."
        echo "Total components: $(python3 -c 'import json,sys; d=json.load(open("sbom.json")); print(len(d.get(\"components\", [])))')"

    - name: Upload SBOM artifact
      uses: actions/upload-artifact@65462800fd760344b1a7b4382951275a0abb4808
      with:
        name: sbom
        path: sbom.json
        retention-days: 90

  security_scan:
    runs-on: ubuntu-latest
    needs: code_generation_fixtures
    permissions:
      contents: read
      security-events: write

    steps:
    - name: Harden Runner
      uses: step-security/harden-runner@9af89fc71515a100421586dfdb3dc9c984fbf411
      with:
        egress-policy: audit
        allowed-endpoints: |-
          github.com:443
          api.github.com:443
          objects.githubusercontent.com:443
          registry.npmjs.org:443
          registry.yarnpkg.com:443
          nodejs.org:443
          *.blob.core.windows.net:443
          *.actions.githubusercontent.com:443
          playwright.azureedge.net:443
          archive.ubuntu.com:80
          security.ubuntu.com:80
    - name: Checkout repository
      uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683

    - name: Download code-generation fixture scripts
      uses: actions/download-artifact@65a9edc5881444af0b9093a5e628f2fe47ea3b2e
      with:
        name: code-generation-fixtures
        path: artifacts/code-generation-fixtures/

    - name: Set up Python
      uses: actions/setup-python@82c7e631bb3cdc910f68e0081d67478d79c6982d
      with:
        python-version: '3.11'

    - name: Install Semgrep
      run: pip install semgrep

    - name: Run Polyglot Security Scan
      run: |
        mkdir -p artifacts/
        semgrep scan --config p/default \
                     --config .semgrep/rules.yml \
                     --exclude="*.html" \
                     --error \
                     --sarif --output=semgrep.sarif \
                     src/ artifacts/

    - name: Upload SARIF report
      uses: github/codeql-action/upload-sarif@dd903d2e4f5405488e5ef1422510ee31c8b32357
      if: always()
      with:
        sarif_file: semgrep.sarif
        category: semgrep
  deploy_preview:
    runs-on: ubuntu-latest
    needs: setup
    if: github.event_name == 'pull_request'
    permissions:
      contents: read
      pull-requests: write
      deployments: write
    steps:
    - name: Harden Runner
      uses: step-security/harden-runner@9af89fc71515a100421586dfdb3dc9c984fbf411
      with:
        egress-policy: audit
        allowed-endpoints: |-
          github.com:443
          api.github.com:443
          objects.githubusercontent.com:443
          registry.npmjs.org:443
          registry.yarnpkg.com:443
          nodejs.org:443
          api.cloudflare.com:443
          *.blob.core.windows.net:443
          *.actions.githubusercontent.com:443
          playwright.azureedge.net:443
          archive.ubuntu.com:80
          security.ubuntu.com:80
    - name: Checkout repository
      uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683

    - name: Set up Node.js
      uses: actions/setup-node@39370e3970a6d050c480ffad4ff0ed4d3fdee5af
      with:
        node-version: '22'

    - name: Download shared environment
      uses: actions/download-artifact@65a9edc5881444af0b9093a5e628f2fe47ea3b2e
      with:
        name: shared-env
        path: .

    - name: Extract shared environment
      run: tar -xf shared-env.tar

    - name: Deploy to Cloudflare Workers Preview
      id: deploy
      env:
        CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
        CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
      run: |
        if [ -z "$CLOUDFLARE_API_TOKEN" ] || [ -z "$CLOUDFLARE_ACCOUNT_ID" ]; then
          echo "Cloudflare secrets are not available. Skipping deployment."
          echo "url=skipped" >> "$GITHUB_OUTPUT"
          exit 0
        fi
        set +e
        OUTPUT=$(npx wrangler deploy --name "equipose-pr-${{ github.event.pull_request.number }}" 2>&1)
        EXIT_CODE=$?
        set -e
        echo "$OUTPUT"
        if [ $EXIT_CODE -ne 0 ]; then
          echo "Wrangler deploy failed with exit code $EXIT_CODE"
          exit $EXIT_CODE
        fi
        OUTPUT_CLEAN=$(echo "$OUTPUT" | sed -r 's/\x1b\[[0-9;]*[mK]//g')
        URL=$(echo "$OUTPUT_CLEAN" | grep -o 'https://[^ ]*\.workers\.dev' | head -1 || true)
        if [ -z "$URL" ]; then URL="Deployment URL not found in output"; fi
        echo "url=$URL" >> "$GITHUB_OUTPUT"

    - name: Comment on PR
      if: steps.deploy.outputs.url != 'skipped' && steps.deploy.outputs.url != ''
      uses: actions/github-script@60a0d83039c74a4aee543508d2ffcb1c3799cdea
      with:
        script: |
          const body = `🚀 PR Preview deployed!\n\n🌐 View here: ${{ steps.deploy.outputs.url }}`;
          const comments = await github.rest.issues.listComments({
            issue_number: context.issue.number,
            owner: context.repo.owner,
            repo: context.repo.repo,
          });
          const existing = comments.data.find(c => c.body.includes('🚀 PR Preview deployed!'));
          if (existing) {
            await github.rest.issues.updateComment({
              comment_id: existing.id,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: body
            });
          } else {
            await github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: body
            });
          }
  lint_actions:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
    - name: Harden Runner
      uses: step-security/harden-runner@9af89fc71515a100421586dfdb3dc9c984fbf411
      with:
        egress-policy: audit
        allowed-endpoints: |-
          github.com:443
          api.github.com:443
          objects.githubusercontent.com:443
          raw.githubusercontent.com:443
    - name: Checkout repository
      uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
    - name: Run actionlint
      run: |
        bash <(curl -s https://raw.githubusercontent.com/rhysd/actionlint/main/scripts/download-actionlint.bash)
        ./actionlint -color
    - name: Check for mutable tags
      run: |
        if grep -rnE 'uses: .*@[vV][0-9]' .github/workflows/; then
          echo "ERROR: Mutable action tags detected. Use SHA-256 hashes instead of tags (e.g., @v1)."
          exit 1
        fi
        echo "All actions are pinned to SHAs."

  release:
    runs-on: ubuntu-latest
    needs: [setup, e2e, code_generation_fixtures, python_script_execution_check, sas_static_validation, stata_static_validation, merge_reports, traceability_matrix, script_validation_report, cross_env_equivalence, sbom, security_scan, lint_actions]
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    permissions:
      contents: write
      issues: write
      pull-requests: write
      id-token: write
      attestations: write
    steps:
    - name: Harden Runner
      uses: step-security/harden-runner@9af89fc71515a100421586dfdb3dc9c984fbf411
      with:
        egress-policy: audit
        allowed-endpoints: |-
          github.com:443
          api.github.com:443
          objects.githubusercontent.com:443
          registry.npmjs.org:443
          registry.yarnpkg.com:443
          nodejs.org:443
          *.blob.core.windows.net:443
          *.actions.githubusercontent.com:443
          playwright.azureedge.net:443
          archive.ubuntu.com:80
          security.ubuntu.com:80
          fulcio.sigstore.dev:443
          rekor.sigstore.dev:443
          tuf-repo-cdn.sigstore.dev:443
          ghcr.io:443
          pkg-containers.githubusercontent.com:443
          token.actions.githubusercontent.com:443

    - name: Checkout repository
      uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
      with:
        fetch-depth: 0

    - name: Install pnpm
      uses: pnpm/action-setup@fc06bc1257f339d1d5d8b3a19a8cae5388b55320
      with:
        run_install: false

    - name: Set up Node.js
      uses: actions/setup-node@39370e3970a6d050c480ffad4ff0ed4d3fdee5af
      with:
        node-version: '22'
        
    - name: Install Dependencies
      run: pnpm install --frozen-lockfile

    - name: Download SBOM artifact
      uses: actions/download-artifact@65a9edc5881444af0b9093a5e628f2fe47ea3b2e
      with:
        name: sbom
        path: .

    - name: Generate Build Provenance
      uses: actions/attest-build-provenance@ef244123eb79f2f7a7e75d99086184180e6d0018
      id: provenance
      with:
        subject-path: 'sbom.json'

    - name: Copy Provenance for Release
      run: cp ${{ steps.provenance.outputs.bundle-path }} ./build-provenance.intoto.jsonl

    - name: Generate SBOM Attestation
      uses: actions/attest-sbom@5026d3663739160db546203eeaffa6aa1c51a4d6
      id: sbom_attest
      with:
        subject-path: 'sbom.json'
        sbom-path: 'sbom.json'

    - name: Copy SBOM Attestation for Release
      run: cp ${{ steps.sbom_attest.outputs.bundle-path }} ./sbom-attestation.intoto.jsonl

    - name: Run semantic-release
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      run: npx semantic-release

